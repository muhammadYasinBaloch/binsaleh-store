# Netlify Redirects
# Netlify auto-enables HTTPS — no force redirect needed

# API routes are handled by Netlify Functions (see netlify.toml)
# No additional redirects needed for API — netlify.toml handles it
