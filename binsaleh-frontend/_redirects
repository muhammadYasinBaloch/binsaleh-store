# Netlify Redirects
# Netlify auto-enables HTTPS — no force redirect needed

# API proxy — routes /api/* requests to Render backend, avoiding CORS entirely
/api/*  https://binsaleh-api.onrender.com/api/:splat  200
